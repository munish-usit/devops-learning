pipeline {
    agent any

    stages {
        
        stage("build") {
            steps {
                echo 'BUILD STEP'
                dir("jenkins-tutorial") {
                    sh "mvn clean install"
                }
            }
        }
        
        stage("test") {
            steps {
                echo 'TEST STEP'
                dir("jenkins-tutorial") {
                    sh "mvn test"
                }
            }
        }
    }
}
